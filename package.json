{
  "name": "mqtt-controller",
  "version": "0.0.0",
  "description": "Allows controlling devices via MQTT.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Terdious and Jessy",
  "license": "",
  "dependencies": {
    "mqtt": "^2.14.0"
  }
}
